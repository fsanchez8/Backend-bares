import { HOST, USER, PASSWORD, DATABASE } from '../../environments/environments';

const CONGIG_DATABASE = {
    host     : HOST,
    user     : USER,
    password : PASSWORD,
    database : DATABASE,
    debug    : false
}

export default CONGIG_DATABASE;